#pragma once
#include "../Utilities/Math.h"
#include <string>
#include <vector>

using namespace std;

class Globals
{
public:
	//date ecran
	static string title;
	static float r, g, b;
	static int screenWidth;
	static int screenHeight;
	static const float frameTime;
};