# Game-Engine-OpenGL
A custom 3D game engine written in C++ using OpenGL and the ESContext framework, featuring a scene manager, resource management system, XML-driven scene loading, terrain generation, skybox rendering, lighting, and particle-based fire effects. The engine implements a modular architecture with separated rendering, scene, and resource systems
